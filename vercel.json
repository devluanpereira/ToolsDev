{
    "builds": [
      {
        "src": "cmd/server/main.go",
        "use": "@vercel/go",
        "config": { "includeFiles": ["go.mod", "go.sum"] }
      }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "cmd/server/main.go" }
    ]
}
  